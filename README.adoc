My dotfiles config. It's very much a work in progress.

== Instructions

. Install
.. https://starship.rs/[Starship]
. Run `install.sh` to start. It _shouldn't_ overwrite anything but I make no guarantees.
